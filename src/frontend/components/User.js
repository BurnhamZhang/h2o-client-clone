import React, {Component} from 'react';

const User = React.createClass({

    render() {
        return (
            <div>
                User
            </div>
        );
    },
});

export default User;